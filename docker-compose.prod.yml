services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - DEBUG=False
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "5172:80"
    volumes: []
    environment: []
