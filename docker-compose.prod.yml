services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - DEBUG=False
    volumes: []
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "5172:80"
    volumes: []
    environment: []
